# Query Parameters Example

This example demonstrates query string handling in c3-api.

- Parsing query parameters from incoming requests
- Accessing values through the request query map
- Using optional query values for filtering and pagination

