# Path Parameters Example

This example shows dynamic route segments using path parameters.

- Defining routes with placeholders like /users/{id}
- Reading extracted values from request params
- Building endpoint logic based on path values

