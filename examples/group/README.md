# Route Group Example

This example demonstrates route groups for organizing endpoints under a shared base path.

- Creating a group with a common prefix
- Registering routes relative to that prefix
- Applying middleware at group scope and endpoint scope

