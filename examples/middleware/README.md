# Middleware Example

This example demonstrates middleware flow and registration.

- Functional middleware: plain functions returning RequestFlow.
- Type method middleware: instance-bound middleware methods.
- Using CONTINUE vs INTERRUPT to allow or stop request processing.
- Attaching middleware to path prefixes.

